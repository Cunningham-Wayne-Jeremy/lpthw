placeholdervar = "{}{}{}{}"

print("{}".format("hello!"))

print(f"{placeholdervar}")

print(placeholdervar.format("What if I only specify one will it work?", "NO it wont","But I do have a question","How can I use this with user input to make an array?"))
